import { Receivecontractdetailinfo } from './receivecontractdetailinfo';

describe('Receivecontractdetailinfo', () => {
  it('should create an instance', () => {
    expect(new Receivecontractdetailinfo()).toBeTruthy();
  });
});
