import { Contractdependinfo } from './contractdependinfo';

describe('Contractdependinfo', () => {
  it('should create an instance', () => {
    expect(new Contractdependinfo()).toBeTruthy();
  });
});
