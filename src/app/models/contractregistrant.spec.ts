import { Contractregistrant } from './contractregistrant';

describe('Contractregistrant', () => {
  it('should create an instance', () => {
    expect(new Contractregistrant()).toBeTruthy();
  });
});
