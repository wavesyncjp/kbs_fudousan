import {Planrentrollhistory} from './Planrentrollhistory';

describe('Planrentrollhistory', () => {
  it('should create an instance', () => {
    expect(new Planrentrollhistory()).toBeTruthy();
  });
});
