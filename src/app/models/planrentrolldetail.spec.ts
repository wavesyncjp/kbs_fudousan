import {Planrentrolldetail} from './Planrentrolldetail';

describe('Planrentrolldetail', () => {
  it('should create an instance', () => {
    expect(new Planrentrolldetail()).toBeTruthy();
  });
});
