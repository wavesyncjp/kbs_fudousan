import { EvictionInfoAttach } from './evictioninfoattach';

describe('EvictionInfoAttach', () => {
  it('should create an instance', () => {
    expect(new EvictionInfoAttach()).toBeTruthy();
  });
});
